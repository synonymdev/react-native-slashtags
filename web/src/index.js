import React from 'react';
import ReactDOM from 'react-dom';
import RNInterface from './Interface';

ReactDOM.render(
  <React.StrictMode>
    <RNInterface />
  </React.StrictMode>,
  document.getElementById('root'),
);
